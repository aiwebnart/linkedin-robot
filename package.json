{
  "name": "linkedin-antimetricas-publisher",
  "private": false,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "check": "node --test --test-concurrency=1",
    "dry-run": "node --env-file=.env src/run-daily.js --dry-run"
  },
  "engines": { "node": ">=20" }
}